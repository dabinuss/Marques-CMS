<!DOCTYPE html>
<html lang="<?= $system_config['admin_language'] ?? 'de'; ?>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Neue Seite erstellen - Admin-Panel - <?= htmlspecialchars($system_config['site_name'] ?? 'marques CMS'); ?></title>
    <link rel="stylesheet" href="/admin/assets/css/marques-panel-style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <script src="/admin/assets/js/tinymce/tinymce.min.js"></script>
    <script src="/admin/assets/js/tinymce-config.js"></script>
</head>
<body>
    <div class="container">
        <!-- SIDEBAR & NAVIGATION -->
        <?php include 'sidebar.phtml'; ?>
        
        <main class="main-content">
            <div class="header">
                <h1 class="admin-page-title">Neue Seite erstellen</h1>
                <div class="header-actions">
                    <a href="<?= htmlspecialchars($helper->appQueryParam('page=page-edit'), ENT_QUOTES, 'UTF-8'); ?>" alt="Neue Seite">
                        <button class="btn">
                            <?= $this->renderIcon('notebook', '', '24'); ?>
                        </button>
                    </a>
                    <a href="<?= htmlspecialchars($helper->appQueryParam('page=blog-edit'), ENT_QUOTES, 'UTF-8'); ?>" alt="Neuer Beitrag">
                        <button class="btn">
                            <?= $this->renderIcon('pencil-plus', '', '24'); ?>
                        </button>
                    </a>
                    <a href="<?= htmlspecialchars($helper->appQueryParam('page=media&action=upload'), ENT_QUOTES, 'UTF-8'); ?>" alt="Hochladen">
                        <button class="btn">
                            <?= $this->renderIcon('file-upload', '', '24'); ?>
                        </button>
                    </a>
                    <a href="../" alt="Website ansehen" target="_blank">
                        <button class="btn">
                            <?= $this->renderIcon('world-star', '', '24'); ?>
                            Website ansehen
                        </button>
                    </a>
                </div>
            </div>
            
            <div class="grid-container">
                <div class="box col-6">
                    <div class="box-header">
                        <h3 class="box-title">Seiten Erstellung</h3>
                    </div>
                    <div class="box-content">
                        <?php if (!empty($success_message)): ?>
                            <div class="alert alert-success">
                                <?= htmlspecialchars($success_message); ?>
                            </div>
                        <?php endif; ?>
                        
                        <?php if (!empty($error_message)): ?>
                            <div class="alert alert-danger">
                                <?= htmlspecialchars($error_message); ?>
                            </div>
                        <?php endif; ?>
            
                        <form method="post" class="admin-form">
                            <input type="hidden" name="csrf_token" value="<?= htmlspecialchars($csrf_token); ?>">
                            <!-- Kein Hidden-Feld für "id", da es eine neue Seite ist -->
                            
                            <div class="form-row">
                                <div class="form-column">
                                    <div class="form-group">
                                        <label for="title" class="form-label">Titel</label>
                                        <input type="text" id="title" name="title" class="form-control" value="" required>
                                    </div>
                                </div>
                                
                                <div class="form-column">
                                    <div class="form-group">
                                        <label for="template" class="form-label">Template</label>
                                        <select id="template" name="template" class="form-control">
                                            <option value="page">Standard-Seite</option>
                                            <option value="landing">Landing-Page</option>
                                            <option value="sidebar">Seite mit Seitenleiste</option>
                                        </select>
                                    </div>
                                </div>
                            </div>
                            
                            <div class="form-group">
                                <label for="description" class="form-label">Beschreibung</label>
                                <input type="text" id="description" name="description" class="form-control" value="">
                                <div class="form-help">Kurze Beschreibung der Seite für SEO-Zwecke.</div>
                            </div>
                            
                            <div class="form-group">
                                <label for="featured_image" class="form-label">Beitragsbild (URL)</label>
                                <input type="text" id="featured_image" name="featured_image" class="form-control" value="">
                                <div class="form-help">Relativer Pfad zum Beitragsbild (z.B. "uploads/image.jpg").</div>
                            </div>
                            
                            <div class="form-group">
                                <label for="content" class="form-label">Inhalt</label>
                                <textarea id="content" name="content" class="form-control content-editor"></textarea>
                            </div>
                            
                            <div class="admin-actions">
                                <button type="submit" class="btn">
                                    <span class="btn-icon"><i class="fas fa-save"></i></span>
                                    Speichern
                                </button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </main>
    </div>
    
    <script nonce="<?= CSP_NONCE ?>">
        document.addEventListener('DOMContentLoaded', function() {
            // TinyMCE initialisieren
            initTinyMCE('.content-editor');
        });
    </script>
</body>
</html>
